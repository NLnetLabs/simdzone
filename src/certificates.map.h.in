#ifndef ZONE_CERTIFICATES_MAP_H
#define ZONE_CERTIFICATES_MAP_H

#define MAP(name, id) { id, name, sizeof(name) - 1 }
static const zone_map_t certificate_map[] = {
@SORTED_LIST@
};
#undef MAP

#endif // ZONE_CERTIFICATES_H
